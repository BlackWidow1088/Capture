import { StyleSheet } from 'aphrodite';
import {
  primaryColor,
  darkerPrimaryColor,
} from '../../style-variables.js';

export default StyleSheet.create({
  squareLogoContainer: {
    outline: 'none',
    padding: '2px',
    background: 'white',
    cursor: 'pointer'
  }
});
